import React from 'react';
import StyledComponent from "../StyledComponent";
import * as s from "./index.module.scss"

const {{parsedName}} = () => {
  return (
    <StyledComponent>{{parsedName}}</StyledComponent>
  );
};

export default {{parsedName}};